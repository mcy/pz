[package]
name = "pz_plugins"
version = "0.1.0"
edition = "2021"

[dependencies]
pz = { path = ".." }

# Use this to break dependency loops.
# pz = { git = "https://github.com/mcy/pz.git" }

heck = "0.4.1"
